import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

// test('renders full name input', () => {
//   render(<App />);
//   const fullname = screen.getByText(/Name/i);
//   expect(fullname).toBeInTheDocument();
// });
